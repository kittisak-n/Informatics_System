import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    criteriaId: null,
    status_login: false,
  },
  mutations: {},
  actions: {},
  modules: {},
});
